org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Cross-drive workaround: the Kotlin daemon's incremental caches break when
# the project and the Gradle/Kotlin home sit on different drives.
kotlin.incremental=false
